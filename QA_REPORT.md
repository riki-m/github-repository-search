# דוח QA וסקירת קוד — Repo Finder

תאריך: 23.09.2026. **מצב: מוכן להגשת מטלת הבית בהיקף המתועד. בדיקת ההתקנה וההרצה מעותק נקי הושלמה בהצלחה.**

## מצב פתיחה והיקף

נבדק העותק `outputs/github-repository-search`, מסמך המטלה המקורי `מבחן מפתח.docx`, הקוד והבדיקות. בתחילת העבודה העץ היה נקי, ללא קבצים לא מנוהלים; ענף `main`, commit `5c10c931aeb295462e930329801d11cea4262b9c`, יעד `https://github.com/riki-m/github-repository-search.git`. אין שינויים קודמים שהתערבבו בעבודה. במהלך האימות הסופי הופיע שינוי נוסף ב־`client/tsconfig.app.json` (הוספת `rootDir`), שלא בוצע במסגרת משימת QA זו; הוא נשאר ללא שינוי ואינו נכלל ב־commit. העותק הנקי נבדק עם גרסת הקובץ שהייתה ב־HEAD, שתישאר גם ב־commit המפורסם.

הסקירה מפרידה בין דרישות המסמך לבין החלטות מימוש ותוספות שאושרו בעבר. לא נוספו מסד נתונים, הרשמה, שמירה בין התחברויות או שכבת שרת חדשה. השרת הפעיל והסשנים הקיימים לא אופסו. לוגיקת השרת לא שונתה; נוספו בו הערות ובדיקות.

## טבלת דרישות

| סיווג ודרישה | מימוש | אימות נוכחי | סטטוס |
|---|---|---|---|
| חובה: Angular 21 ומעלה | `client/package.json`, lockfile | Angular 21.2; בדיקות ובניית Production | תקין |
| חובה: .NET 10 | `server/RepositorySearch.Api/RepositorySearch.Api.csproj` | net10.0; בנייה ו־84 בדיקות | תקין |
| חובה: Material או Bootstrap | רכיבי Angular Material בטפסים, לשוניות וחלונית | בנייה, בדיקות רכיבים ודפדפן | תקין |
| חובה: כפתור ו־Enter | `client/src/app/explorer.html`, `explorer.ts` | חיפוש HILAN ב־Enter ו־USER בכפתור | תקין |
| חובה: GitHub דרך השרת | `Controllers/RepositoriesController.cs`, `Services/GitHubSearch.cs` | שש השוואות חיות; בדיקות כתובות וקידוד | תקין |
| חובה: גלריה, שם, תמונת בעלים, Bookmark | `repository-card.ts`, `repository-card.html` | דפדפן ובדיקות משותפות לשתי הלשוניות | תקין |
| חובה: JWT | `Program.cs`, `Authentication.cs`, `auth.service.ts` | חסר/פג/לא תקין, בעלות סשן, התנתקות והגבלת העברת הטוקן ל־API המקומי | תקין בכיסוי המתועד |
| חובה: Custom Session | `Services/SessionStore.cs` | תפוגה, בידוד משתמשים/כניסות, קיבולת ונעילה | תקין |
| חובה: אובייקט GitHub מלא בסשן | `Remember`, `Bookmark`, `ApiTests.cs` | JSON מקונן ושדות שאינם נשלחים לגלריה נשמרים | תקין |
| חובה: README והרצה | `README.md` | פקודות, שני מסופים, פורטים, משתמשים, Session ותקלות נבדקו מול הקוד | התקנה והרצה מעותק נקי עברו |
| חובה: קוד נקי והערות | אחריות מחולקת ל־API, שירותים, מצב ותצוגה | סקירת קוד, חילוץ תבנית משותפת, הסרת הגדרת דיבוג ישנה | תקין; אין צורך בשכתוב |
| בונוס: מסך מועדפים נפרד | `bookmarks.ts`, לשונית Bookmarks | שמירה, כרטיס מלא, רענון, מצב ריק, שגיאה וניסיון חוזר | כבר קיים ונבדק |
| תוספות מאושרות: עימוד/מיון/שם בלבד/הדרכה | `explorer.*`, `SearchRanking.cs`, `SearchScope.cs` | בדיקות מבוקרות והשוואה חיה | תקין בכיסוי המתועד |
| הגשה: GitHub וקישור | origin/main הקיימים | היעד אומת; סקירת ההבדלים לפני פרסום | היעד אומת; הפרסום מבוצע לענף הקיים לאחר אימות |

הנתיבים היחסיים בטבלה הם בתוך `client/src/app` או `server/RepositorySearch.Api` לפי ההקשר. הדגמת JWT בעזרת שני משתמשים קבועים היא החלטת מימוש, לא דרישה להוסיף מערכת ניהול חשבונות.

## ממצאים ותיקונים לפי חומרה

לא נמצאה בכיסוי שבוצע תקלה קריטית מוכחת בשמירה, בבידוד סשנים או בהעברת החיפוש.

| עדיפות | ממצא מאומת | טיפול וראיה |
|---|---|---|
| בינונית | מגבלת 1,000 נעלמה לאחר ההדרכה | המגבלה נשארת בסיכום התוצאות; בדיקת רגרסיה ודפדפן USER/עמוד אחרון |
| בינונית | בזמן טעינה הכרטיסים הקודמים נשארו בלי סיכום השאילתה; כישלון היה עלול להתפרש כתוצאות חדשות | הסיכום נשאר ותוצאות קודמות מסומנות; בדיקת טעינה וכישלון |
| בינונית | Popular & active מסנן גם ארכוב ופעילות ללא הסבר ליד הבחירה | הסבר קצר רק בבחירת מצב זה; מודגש שכוכבים אינם מדד לאיכות קוד |
| נמוכה | תמונת בעלים חסרה/שבורה ללא חלופה | אות ראשונה ושם בעלים; בדיקת אירוע error ונתונים אופציונליים חסרים |
| נמוכה | אחסון דפדפן עם טוקן ריק יכול היה להיראות כהתחברות | ערכים ריקים נפסלים; בדיקות אחסון פגום ופג תוקף |
| נמוכה | הגדרת VS Code הפנתה ל־Karma ב־9876 אף שהבדיקות ב־Vitest | הוסרה הגדרת Karma; משימת בדיקה חד־פעמית תואמת לכלי הנוכחי |
| שער אימות נסגר | לא היה מקום בטוח להתקנה כפולה בכונן C | באישור המשתמשת נוצר עותק מבודד ב־D; התקנה, בנייה, בדיקות והרצה בדפדפן עברו |
| המלצה, אינה תקלה פונקציונלית | חבילת פתיחה מעל תקציב האזהרה | 550.16KB מול 500KB; לא הוגדל הסף. טעינה דחויה היא שיפור עתידי אפשרי, לא חלק מתיקון נדרש |

## Clean Code, Comments וניקוי

- תבנית כרטיסיית המאגר חולצה מ־`repository-card.ts` ל־`repository-card.html`; אותו רכיב ממשיך לשמש חיפוש ומועדפים.
- נוספו הערות על החלופה לתמונה, זמינות מגבלת GitHub לאחר החלונית, הנעילה האטומית בשמירת מועדפים ואימות בעלות סשן בנוסף לחתימת JWT.
- הוסר **רק** `client/src/app/app.scss`: היה ריק, וההפניה היחידה הוסרה מ־`app.ts`.
- הוחלפו הגדרות עורך מיושנות; לא נמחקו מסמכי מטלה, קובצי משתמש, היסטוריית Git, תלויות או תיקיות מחוץ לפרויקט.
- נבדקו שמות וחלוקת אחריות; לא נמצאה הצדקה לשינוי שמות גורף או להוספת שירות מצב נוסף. אין TODO/FIXME פתוח בקוד שנבדק.
- README כולל הורדת הפרויקט, פתרון תקלות, הסבר קבוע על אורך חיי המועדפים והפניה לדוח זה. ההיסטוריה ב־VERIFICATION מופרדת מהתוצאות הנוכחיות.

## בדיקות אוטומטיות ותוצאות

**שרת: 84 עברו, 0 נכשלו, 0 דולגו.** הרצה עם תיקיית artifacts נפרדת, בלי לעצור את API הפעיל. הכיסוי כולל:

- חיפוש, שם בלבד, מיון, qualifiers סותרים, עמודים לא תקינים ושגיאת סכמת GitHub לפני הכנסת נתונים לסשן.
- קידוד רווחים, מקפים, עברית, Unicode, מירכאות, `&`, `+`, `%`, `?`; 0/200/201 תווים. אלה בדיקות שימור/ולידציה, לא התחייבות לתוצאות התאמה של GitHub לכל טקסט.
- שמירה כפולה ומקבילה, JSON מקונן, הפרדת משתמשים והתחברויות, תפוגה, 1,000 Sessions ו־100 סימניות. תחרות של 120 שמירות הסתיימה ב־100 הצלחות ו־20 דחיות קיבולת.
- פינוי מטמון החיפוש אינו מוחק מועדפים; פריט שאינו במטמון ואינו שמור מחייב חיפוש מחדש.
- אימות, חסרים ואורכי כניסה, JWT שאינו שייך למשתמש הסשן, ביטול אחרי התנתקות, no-store, מגבלות בקשות.
- GitHub 403/429/422/500, תשובה פגומה, כשל רשת ו־timeout; שגיאה אינה מוצגת כאפס תוצאות מוצלחות.
- בדיקת לוגים לזרימת כניסה וחיפוש ללא סיסמה, טוקן, שאילתה או גוף תשובה.

**לקוח: 42 בדיקות עוברות** בחמש קבוצות. כולל גבולות 0, 1, 30, 31, 999, 1,000 ו־1,001; טווחים וכרטיסים, תוצאות חלקיות, שאילתה/מיון/סינון שהשתנו ללא שליחה, בקשות מבוטלות, ירידת total_count והתאוששות מוגבלת, שגיאה וניסיון חוזר, כשל שמירה, שני סדרי הגעה של טעינה/אישור שמירה, מעבר לשוניות ללא HTTP חדש, אחסון פגום, התנתקות/timeout ותמונת בעלים שבורה.

**Production build עבר** עם אזהרת גודל מתועדת. אין כאן מדידת זמן תגובה או הוכחת עומס Production. ההגנה על ביצועים היא מבנית: עמוד בודד לבקשה, ביטול חיפוש ישן, ללא הורדת כל התוצאות, ללא העשרת כל כרטיס בבקשות נוספות, תמונות lazy ומצב חיפוש הנשמר בין לשוניות.

## בדיקות חיות מול GitHub

קריאות אנונימיות, API מקומי, 30 פריטים לעמוד, באותו רצף זמן. בכל שש ההשוואות: HTTP 200 משני הצדדים, `incomplete_results=false`, מספרים וסדר מזהים זהים וכל שדות הכרטיסים זהים. ראיות וכתובות מדויקות בקובצי JSON המצורפים.

| שאילתה/מצב | עמוד | סך התאמות | פריטים | נמצא riki-m/Hilan-Test |
|---|---|---|---|---|
| HILAN / Default | 1 | 503 | 30 | לא |
| HILAN / Default | 2 | 503 | 30 | לא |
| HILAN-TEST in:name | 1 | 3 | 3 | כן |
| repo:riki-m/Hilan-Test | 1 | 1 | 1 | כן |
| HILAN / Recently updated | 1 | 503 | 30 | כן |
| HILAN / Popular & active | 1 | 235 | 30 | לא |

30 שמות המאגרים בגלריית HILAN תאמו בסדרם לראיות ה־API. המאגר המבוקש נגיש בחיפוש אנונימי ממוקד: זו ראיה לנראות ציבורית. לא נדרשה הזדהות GitHub אישית. אין להסיק מכך מיקום קבוע בחיפוש רחב; לא סרקנו את כל 17 העמודים בסקירה זו.

לפי [תיעוד GitHub](https://docs.github.com/en/search-github/searching-on-github/searching-for-repositories), ברירת המחדל מחפשת בשם, בתיאור ובנושאים. `in:name` מצמצם שדה ואינו בדיקת שוויון שם; `user:` מצמצם בעלים ו־`repo:` מזהה מאגר מסוים. אין רגישות לאותיות גדולות/קטנות. לא הומצא הסבר לטוקניזציה פנימית של מקפים. [תיעוד Search API](https://docs.github.com/en/rest/search/search#search-repositories) הוא המקור למיון, לעימוד ולמגבלת 1,000.

## דפדפן, Session וחוויית משתמש

נבדקו כניסה, Enter, כפתור Search, חלונית, Escape, שמירה ומעבר לשונית, רענון המשחזר מועדף, וכניסה חדשה לאותו משתמש המתחילה באפס. נוסח הכניסה וה־README תואם להתנהגות: Refresh באותה לשונית ממשיך Session תקף; כניסה חדשה אינה שחזור חשבון קבוע.

נבדקו חיפוש רחב וחיפוש Name only. בשניהם מופיעה עצת פירוט החיפוש; ההמלצה לסמן Name only מופיעה רק כשאינו מסומן. החלונית אינה נפתחת שוב באותה כניסה. אחרי סגירתה מגבלת 1,000 נשארה בסיכום. עמוד Last של USER הציג 991–1000 ו־Next לא פעיל. עריכת המיון בלי שליחה לא שינתה את מיון העמוד שהתקבל.

נבדקו מובייל 390x844 ומחשב 1280x900, ללא גלילה אופקית במדידת רוחב המסמך. ניווט מקלדת בין לשוניות עובד; יש תוויות לטפסים ולכפתורים. תמונות חסרות ונתונים חסרים נבדקו ברכיב. בדיקה זו אינה ביקורת WCAG מלאה או בדיקת קורא מסך.

## מגבלות, פרטיות ושער פרסום

- **התקנה והרצה מעותק נקי עברו:** באישור המשתמשת נוצר `D:\RepoFinder-QA-20260923` ממקורות בלבד, ללא node_modules/bin/obj. בוצעו `npm ci` (471 חבילות) ו־`dotnet restore` עם מטמונים חדשים בתוך תיקיית הבדיקה; Node 24.12.0 ו־SDK 10.0.204. כל 84 בדיקות השרת ו־42 בדיקות הממשק עברו שם, וכן בניית Production. זו בדיקת עותק נקי על אותו מחשב, לא מכונה וירטואלית חדשה.
- בדיקת שגיאות רשת/קיבולת/תפוגה נעשתה בעיקר בתחליפים ובשרת בדיקות, ולא באמצעות פגיעה בשרת המשתמש. לא כל צירוף אפשרי של תקלות או שינוי נתוני GitHub נבדק.
- אין סיסמאות אמת או PAT שנוספו. `demo1`/`demo2` וסיסמאותיהם הם fixtures ציבוריים מכוונים, גלויים בקוד/README ומוסברים שם; זו אינה הצפנה או מערכת זהויות לשימוש מבצעי. האיסור על סודות אינו סיבה להסתיר מהבודק את פרטי הדגמה הנדרשים.
- JWT ב־sessionStorage הוא בחירה ידועה להדגמה; אינו מגן מפני JavaScript זדוני באותו origin. אין כאן אישור אבטחה לשימוש במידע משטרתי אמיתי. לא נוספו שכבות Production מעבר למטלה.
- לא בוצעו בדיקת חדירה, ביקורת כל הלוגים החיצוניים, עומס ממושך או אימות private repositories עם PAT.
- שער העותק הנקי נסגר לפני הכנת commit. היעד המאומת הוא `origin/main` במאגר הציבורי הקיים; fetch אישר שאין סטייה מהענף המרוחק. אין force push ולא נשלח דוא"ל. מזהה ה־commit ותוצאת ה־push מדווחים בהודעת הסיום לאחר הפעולה.

בסביבת הבדיקה בלבד הוחלפו הפורטים ל־5081/4201 וה־proxy הופנה לשרת המבודד. נבדקו כניסה, חיפוש חי של `repo:riki-m/Hilan-Test`, שמירה, רענון המשמר מועדף, וכניסה חדשה לאותו חשבון המתחילה באפס. בסיום בוצעה יציאה ותהליכי הבדיקה בלבד נעצרו. המקור והשרת הפעיל לא שונו לצורך בידוד הפורטים. קריסת מעטפת PowerShell בניסיון ראשון תועדה; הרצה חוזרת נפרדת של בדיקות השרת הצליחה. קובצי התקנה ומטמונים מבודדים נשארו ב־D ולא צורפו ל־Git.
